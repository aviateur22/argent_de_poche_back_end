spring.config.import=classpath:database.properties

#Pour l'acces aux image depuis un repertoir externe
folder.image.path=@folder.path@

# Version api
api.version=@api.version.path@

#Cors
cors.domains=@cors.domains@

# Image de default
default.child.image.name=@default.child.image.name@
default.image.extension=@default.image.extension@

#JWT
jwt.issuer=@jwt.issuer@
jwt.validity.hour=@jwt.validity.hour@
jwt.secret.key=${JWT_SECRET}

# Horaire
zone.id=@zone.id@

# FrontEnd
front.end.base.url=@front.end.base.url@
qr.code.child.account.url=@qr.code.child.account.url@